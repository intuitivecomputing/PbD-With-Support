#include "ros/ros.h"

*** Action server ***/
void execute(const demoshop_ubuntu::ExecutePathGoalConstPtr& goal, Server* as)
{
  
}

int main(int argc, char **argv)
{
  ros::init(argc, argv, "baseline_interface");
  ros::NodeHandle node_handle;

  ros::AsyncSpinner spinner(2); 
  spinner.start();

  ros::waitForShutdown();
  
  return 0;
}
